.main0{'data-message-id': message.id}
  .main1 
    = message.user.name
    .main2x
      = message.created_at.strftime("%Y/%m/%d %H:%M")
  .main3x
    - if message.content.present?
      %p.main3x
      = message.content
  = image_tag message.image.url, class: 'contents-main' if message.image.present?